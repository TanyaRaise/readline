language: csharp  
sudo: required  
dist: trusty  
mono: none
dotnet: 2.1.300
before_script:
  - dotnet restore
script:    # the following commands are just examples, use whatever your build process requires
  - ./build.sh
  - ./test.sh
